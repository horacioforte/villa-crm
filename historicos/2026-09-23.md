# Histórico 2026-09-23

_Falha ao buscar dados do CRM: a ferramenta web_fetch recusou a URL https://villa-crm.vercel.app/api/auditoria/resumo-diario?formato=md&data=2026-09-23 ("URL not in provenance set" — só busca URLs que aparecem numa mensagem do usuário ou num resultado de busca). Além disso, a web_fetch não permite enviar o header `Authorization: Bearer ...`. Observação: a execução de 2026-09-22 recebeu HTTP 401 ("Autenticacao obrigatoria"), então o CRON_SECRET também pode estar desatualizado. Recomenda-se rever o método de coleta desta tarefa agendada (ex.: endpoint que aceite o token via query string, ou um cron no próprio Vercel gravando o histórico)._
